{
  "name": "program-todolistapp",
  "version": "1.0.0",
  "description": "Aplikasi TodoList berbasis vanilla HTML/CSS/JavaScript",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "fast-check": "^3.22.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
